<?php
/*
|--------------------------------------------------------------------------
| Simple Hyper Link + Short Link
|--------------------------------------------------------------------------
*/

date_default_timezone_set("Asia/Jakarta");

/*
|--------------------------------------------------------------------------
| URL Website
|--------------------------------------------------------------------------
| Ganti dengan domain kamu
*/

define("SITE_URL", "https://condogames.my.id");

/*
|--------------------------------------------------------------------------
| API KEY
|--------------------------------------------------------------------------
*/

define("API_KEY", "CIMERX1234");

/*
|--------------------------------------------------------------------------
| Database JSON
|--------------------------------------------------------------------------
*/

define("DB_FILE", __DIR__ . "/database.json");

/*
|--------------------------------------------------------------------------
| Panjang Short Code
|--------------------------------------------------------------------------
*/

define("SHORT_LENGTH", 6);

/*
|--------------------------------------------------------------------------
| Membuat database jika belum ada
|--------------------------------------------------------------------------
*/

if (!file_exists(DB_FILE)) {
    file_put_contents(DB_FILE, "{}");
}

/*
|--------------------------------------------------------------------------
| Membaca Database
|--------------------------------------------------------------------------
*/

function readDatabase()
{
    return json_decode(file_get_contents(DB_FILE), true) ?: [];
}

/*
|--------------------------------------------------------------------------
| Menyimpan Database
|--------------------------------------------------------------------------
*/

function saveDatabase($data)
{
    file_put_contents(
        DB_FILE,
        json_encode($data, JSON_PRETTY_PRINT | JSON_UNESCAPED_SLASHES)
    );
}

/*
|--------------------------------------------------------------------------
| Membuat Random Code
|--------------------------------------------------------------------------
*/

function generateCode($length = SHORT_LENGTH)
{
    $chars = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789";
    $code = "";

    for ($i = 0; $i < $length; $i++) {
        $code .= $chars[random_int(0, strlen($chars) - 1)];
    }

    return $code;
}

/*
|--------------------------------------------------------------------------
| Validasi URL
|--------------------------------------------------------------------------
*/

function isValidUrl($url)
{
    return filter_var($url, FILTER_VALIDATE_URL)
        && str_starts_with($url, "https://");
}
?>